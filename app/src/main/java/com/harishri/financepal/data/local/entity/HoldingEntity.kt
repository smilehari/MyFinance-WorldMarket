package com.harishri.financepal.data.local.entity

//This is your main portfolio data
//P&L calculation, Top gainers/losers
//Current stock positions (what stocks user owns)
//Tracks quantity, average price, current price

/*
@Entity(tableName = "holdings", primaryKeys = ["userId", "symbol"])
data class HoldingEntity (
    @PrimaryKey
    val userId: String,
    val symbol: String,                    // e.g., "RELIANCE", "TCS"
    val companyName: String,               // e.g., "Reliance Industries Limited"
    val exchange: String,                  // NSE, BSE
    val quantity: Double,
    val averagePrice: Double,              // Average buying price
    val currentPrice: Double = 0.0,        // Last fetched market price
    val lastPriceUpdate: Long = 0L,        // When price was last updated
    val createdAt: Long = System.currentTimeMillis(),
    val updatedAt: Long = System.currentTimeMillis()
)*/
