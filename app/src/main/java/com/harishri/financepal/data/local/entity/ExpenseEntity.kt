package com.harishri.financepal.data.local.entity

/*
@Entity(
    tableName = "expenses",
    indices = [
        Index(value = ["userId"]),
        Index(value = ["category"]),
        Index(value = ["expenseDate"])
    ]
)
//Personal finance dashboard
data class ExpenseEntity(
    @PrimaryKey
    val id: String = UUID.randomUUID().toString(),
    val userId: String,
    val category: ExpenseCategory,
    val subcategory: String? = null,       // Optional subcategory
    val amount: Double,
    val description: String,
    val expenseDate: Long,                 // When expense occurred
    val isRecurring: Boolean = false,
    val recurringPeriod: RecurringPeriod? = null, // MONTHLY, YEARLY, etc.
    val paymentMethod: String? = null,     // Cash, Card, UPI, etc.
    val createdAt: Long = System.currentTimeMillis(),
    val updatedAt: Long = System.currentTimeMillis()
)*/
